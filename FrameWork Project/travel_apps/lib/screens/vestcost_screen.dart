import 'package:flutter/material.dart';

class ViewEstimateCostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Estimate Cost'),
      ),
      body: Center(
        child: Text('View Estimate Cost Screen'),
      ),
    );
  }
}
